    // Define the static IP address, gateway, subnet mask and DNS
    // This is the fixed IP address to use in my local network
    IPAddress staticIP(192, 168, 1, 248);
    IPAddress gateway(192, 168, 1, 1);
    IPAddress subnet(255, 255, 255, 0);
    IPAddress dns1(216, 230, 147, 90);
    IPAddress dns2(216, 230, 139, 8);